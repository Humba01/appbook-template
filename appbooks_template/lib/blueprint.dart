import 'package:flutter/material.dart';

class ScrollbarComponentCore extends StatelessWidget {
  const ScrollbarComponentCore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: const Text('Scrollbar'),
      ),
    );
  }

}
